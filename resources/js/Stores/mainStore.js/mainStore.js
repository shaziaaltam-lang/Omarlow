import { defineStore } from 'pinia';export const useMainStore = defineStore('main', {  state: () => ({    isAuthenticated: false,    user: null,    notifications: []  }),  actions: {    login(userData) {      this.isAuthenticated = true;      this.user = userData;    },    logout() {      this.isAuthenticated = false;      this.user = null;      this.notifications = [];    },    addNotification(notification) {      this.notifications.push(notification);    }  }});